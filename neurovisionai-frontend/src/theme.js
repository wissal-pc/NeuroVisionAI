// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#1976d2' }, // Bleu IRM
    secondary: { main: '#90caf9' }
  },
});

export default theme;
